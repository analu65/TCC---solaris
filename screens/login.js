import { StyleSheet, View, Text, Button, ImageBackground } from "react-native";
export default function Login(){
    return(
        <View>
            <Text>isso eh o login</Text>
        </View>
    );
}